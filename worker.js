const json=(value,status=200,cache='no-store')=>new Response(JSON.stringify(value),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':cache}});const valid=x=>{try{return /^CAMP-\d{2,4}$/.test(x.code)&&x.name&&/^\d{4}-\d{2}-\d{2}$/.test(x.endDate)&&/^https?:$/.test(new URL(x.url).protocol)}catch{return false}};async function api(request,env,path){if(!env.DB)return json({error:'DB not configured'},503);if(request.method==='GET'){const setting=await env.DB.prepare("SELECT value FROM settings WHERE key='columns'").first(),rows=await env.DB.prepare('SELECT code,name,end_date AS endDate,url,active,sort_order AS `order` FROM campaigns ORDER BY sort_order,code').all();return json({columns:Number(setting?.value)||3,campaigns:(rows.results||[]).map(x=>({...x,active:Boolean(x.active)}))},200,'public,max-age=30,s-maxage=60,stale-while-revalidate=300')}if(request.method==='POST'&&path===''){const x=await request.json();if(!valid(x))return json({},400);await env.DB.prepare('INSERT INTO campaigns(code,name,end_date,url,active,sort_order) VALUES(?,?,?,?,1,(SELECT COUNT(*)+1 FROM campaigns))').bind(x.code,x.name,x.endDate,x.url).run();return json({ok:true},201)}if(request.method==='PUT'&&path==='settings'){const x=await request.json(),n=x.columns===4?4:3;await env.DB.prepare("INSERT INTO settings VALUES('columns',?) ON CONFLICT(key) DO UPDATE SET value=excluded.value").bind(String(n)).run();return json({ok:true})}if(request.method==='PUT'&&path){const x=await request.json();if(!valid(x))return json({},400);await env.DB.prepare('UPDATE campaigns SET name=?,end_date=?,url=?,active=? WHERE code=?').bind(x.name,x.endDate,x.url,x.active===false?0:1,path).run();return json({ok:true})}if(request.method==='DELETE'&&path){await env.DB.prepare('DELETE FROM campaigns WHERE code=?').bind(path).run();return json({ok:true})}return json({},404)}export default{async fetch(request,env){const u=new URL(request.url);if(u.pathname==='/api/campaigns'||u.pathname.startsWith('/api/campaigns/'))return api(request,env,decodeURIComponent(u.pathname.slice('/api/campaigns/'.length)));return env.ASSETS.fetch(request)}};
